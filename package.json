{
  "name": "big-data-tree",
  "version": "1.0.0",
  "description": "Generate trees based of activities in elasticsearch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --progress --profile --colors",
    "dev": "webpack-dev-server --progress --profile --colors --hot"
  },
  "keywords": [
    "elasticsearch",
    "canvas"
  ],
  "author": "KFS",
  "license": "MIT",
  "dependencies": {
    "elasticsearch": "^11.0.1"
  },
  "devDependencies": {
    "babel-core": "^5.5.8",
    "babel-loader": "^5.1.4",
    "webpack": "^1.12.3",
    "webpack-dev-server": "^1.9.0"
  }
}
